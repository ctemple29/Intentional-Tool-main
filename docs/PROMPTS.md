You are building a simple site capable of fitting to all screens and imputs are to be compatable with touch screens. On this site you are building a mechanical calculator capable of basic and simple calculations, nothing more. There are the following files: setupCanvas.js where all rudimentary Canvas setup goes; loop.js where any animation loops are placed; imput.js where all code relating to reacting to the user's imput is placed; math.js, where any mathematical data is placed; main.js where any js code that does not fall into the categories listed above is placed; index.html, the html file this all runs to; and style.css where any code relating to affecting the appearance and style of the site is placed. All responses from the AI must be followed by a simple breakdown of what was changed and where. Prompts: There is an error in (x) function of the code, please isolate the problem and fix; Please impliment/add (x) function; Adjust (x) function of the code to better fit theme/idea.
Running Inital Prompt:
Greetings, I would like you to create a simple HTML website capable of HiDPI scaling. Using the information provided in PROMPTS.md, please create a calculator capable of multiplication, division, addition, and subtraction between two numbers. In all calculations please round to the nearest thousandth and IF the number was rounded, make the final thousandths digit bold. The method to imput the two digits should be as follows: near the bottom of the screen there should be two wheel cranks where when spun clockwise, a number, which shall be placed directly above the crank, shall go up, when rotated counterclockwise, the number shall go down. When rotated for longer than three seconds in either direction, the speed of the digit incriments shall go up and reset when rotation halts. The digits should be displayed directly above the cranks and there should be a gap between the two cranks and their respective digits where a small lever could be pulled to switch between 'normal' mode and 'precision' mode. In precision mode, the rotation of the cranks are slowed down in order to carefully input decimals or final digits. On the right side of the screen, above the digits displayed, there should be two small levers which when pulled downwards reset the digits created through the crank back to zero. Each lever should be labeled 'Reset 1' (left) and 'Reset 2' (right). Just above the right reset lever there should be a dial where when physically turned you can select your operation (addition, subtraction, multiplcation, or division) and the selected operation should be displayed in green text. Near the top of the screen, still focused on the right-hand side, there should be another lever which rests turned to the right, when pulled to the left, it should end facing down (exactly 90 degrees) and when that is done the answer should be calculated. The answer should not be displayed immediately and instead 'printed' out along the left hand of the screen in a small box. This should take a duration of 2.5 seconds. For style, keep this initial form very basic, only create the foundations for the program utilsing grayscale colouration and basic movements. For example: the wheels utilised should be a circle with a smaller circle where could be seen as a 'handle' which is used to turn the wheel. 

Prompt 2:Some minor tweaks, could you make the reset levers be pulled down instead of up? After that, could you make the 'calculate' lever one whole lever which moves clockwise 90 degrees when pulled?
 Prompt 3: Okay one thing at a time, now the levers for the reset button clip into the wheels and cover up the digit. Could you make it so that the reset label is closer to the wheel and the levers rest flat and are pulled upward with mouse movement.
 Prompt 4: Could you move the 'calculate' label upward so it no longer is clipping into the lever?
 Prompt 5: the 'Calculate' label is still too low. Raise it by 20 pixels please.
 Prompt 6: Could you make the rotation when pulled significantly less? Keep the resting orientation the exact same but only change how much it rotates. Give me exact values for the rotation alteration for later iteration.